# Site redesign implementation

1. Add failing build tests for home composition and copying the canonical app icon.
2. Add a home-only HTML composition around Markdown content; keep legal prose rendering.
3. Implement shared branding, responsive styling, accessible FAQ and outcome-led copy.
4. Trigger Pages rebuilds on app icon changes; update Unreleased.
5. Build, validate local links/assets, run script/Swift tests and audit; visually inspect
   the generated site through Peekaboo where permissions permit.
